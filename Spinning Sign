#pragma config(Sensor, in1,    pot,            sensorPotentiometer)
#pragma config(Sensor, in2,    lineTracker,    sensorReflection)
#pragma config(Sensor, dgtl1,  bumpSwitch,     sensorTouch)
#pragma config(Sensor, dgtl2, red,            sensorLEDtoVCC)
#pragma config(Motor,  port1,           sign,          tmotorVex393_HBridge, openLoop)
//*!!Code automatically generated by 'ROBOTC' configuration wizard               !!*//

/*
Project Title: Spinning Sign
Team Members: Mr. Awesome
Date:8/9/2017
Section: UTT AR CTI


Task Description:
Program the spinning sign to turn on when the line tracker senses daylight. The sign will
turn off at dark or if the emergency stop switch is pressed. The potentiometer will serve
as a dimmer varying the speed of the sign.


Pseudocode:
Repeat forever
Wait until the line tracker senses daylight (dark)
Motor turns on at a variable speed based on the potentiometer
If the limit switch is pressed, turn motor off and LED on
When the limit switch is pressed again, turn LED off and if it is daylight, start motor


*/

task main()
{                                     //Program begins, insert code within curly braces
	turnLEDOff(red);
	while(true)
	{
		if(SensorValue[bumpSwitch]==1)
		{
			stopMotor(sign);
			turnLEDOn(red);
			wait(1);
			untilBump(bumpSwitch,500);
			turnLEDOff(red);
		}
		else if(SensorValue[lineTracker]<1400)
		{
			stopMotor(sign);
		}
		else
		{
			startMotor(sign,SensorValue[pot]/32);
		}
	}
}

